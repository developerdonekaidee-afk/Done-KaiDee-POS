# แผนรวมระบบฟิตเนสเข้ากับ POS หลัก

อ่านโค้ดฟิตเนสครบแล้ว (4 ส.ค. 2569) — `fitness-pos.jsx` 1,794 บรรทัด 41 คอมโพเนนต์
รวมไฟล์ประกอบอีก 40 คอมโพเนนต์ · โครงข้อมูล 20 ก้อน

---

## ข้อสรุปที่ต่างจากที่เคยเข้าใจ

**ฟิตเนสไม่ใช่ต้นแบบ** — เป็นระบบที่เขียนจบแล้ว มีหลังบ้าน sync ข้ามเครื่องจริง
(`PUT/GET /doc/fitness?biz=<ยิม>` บน platform-worker มี `rev` คุมเวอร์ชัน)

**และบางส่วนทำได้ดีกว่าฝั่งร้านอาหาร** — ดูหัวข้อ "ของที่ฟิตเนสมีแต่ร้านอาหารไม่มี"
งานนี้จึงไม่ใช่ "ยกของดีไปให้ฟิตเนส" แต่เป็นการรวมสองทางที่ต่างฝ่ายต่างมีของ

---

## เทียบทีละก้อนข้อมูล

| ก้อนข้อมูลฟิตเนส | ตรงกับฝั่งร้านอาหาร | ตัดสิน |
|---|---|---|
| `products` | `menu` (D1) | 🔄 ย้ายไปใช้ของกลาง |
| `raws` | `raw` (D1) | 🔄 ย้าย |
| `purchases` | `purchases` (D1) | 🔄 ย้าย |
| `orders` | `orders` (D1) | 🔄 ย้าย |
| `renewals` เฉพาะ `kind:'shop'` | `sales` (D1) | 🔄 ย้าย |
| `renewals` เฉพาะ `kind:'renew'` | — | ✅ เก็บไว้ (ต่ออายุแพ็ก เป็นของยิม) |
| `dayCloses` | `cash_days` (D1) | 🔄 ย้าย |
| `staff` · `staffShifts` | ทะเบียนพนักงาน | 🔄 ย้าย |
| `promos` | `promos` (D1 · ทำเสร็จ 3 ส.ค.) | 🔄 ย้าย — ของกลางคิดยอดฝั่งเซิร์ฟเวอร์ ปลอดภัยกว่า |
| `voucherDefs` · `vouchers` | ❌ **ฝั่งร้านอาหารไม่มี** | ⬆️ **ยกขึ้นเป็นของกลาง ให้ร้านอาหารได้ใช้ด้วย** |
| `members` | `members` (D1) | ⚠️ **รวมแบบระวัง — คนละความหมาย** |
| `packages` `trainers` `classes` `checkins` `ptBookings` | — | ✅ ของยิมล้วน ไม่แตะ |
| `gym` | `shops` | 🔄 รวมเป็นร้านเดียวกัน |
| `plan` · `addons` | แพ็กเกจ/ใบอนุญาต | ⚠️ ต้องตัดสินใจเรื่องราคาก่อน |

---

## ⚠️ 3 เรื่องที่ต้องตัดสินใจก่อนเขียนโค้ด (สำคัญกว่าตัวโค้ด)

### 1. โมเดลราคาขัดกันเอง
ฟิตเนสมีระบบเก็บค่าธรรมเนียมต่อธุรกรรมฝังอยู่ — `fitness-data.jsx` มี `PRICING` 3 แบบ
และฟังก์ชัน `appFee()` หัก **1–2.5% ของทุกยอดที่จ่ายผ่านแอป** (ต่ออายุ · PT · ขายของ)
ค่าเริ่มต้นของยิมสาธิตคือแบบ C = ฿990/เดือน + 1%

**ขัดกับฝั่งร้านอาหารที่ประกาศ zero-GP ไม่หักต่อบิลเลย** และเป็นจุดขายหลักที่ใช้สู้ Grab

ต้องเลือกทางเดียว: ยิมก็ zero-GP เหมือนกัน · หรือคิดค่าธรรมเนียมเฉพาะสายฟิตเนส
(ถ้าเลือกอย่างหลัง ต้องอธิบายให้ได้ว่าทำไมสองสายไม่เหมือนกัน)

### 2. "สมาชิก" สองความหมาย
- ยิม: แพ็กที่ซื้อแล้ว **มีวันหมดอายุ** → หมดอายุ = เข้ายิมไม่ได้
- ร้านอาหาร: **สะสมแต้ม/ระดับ** ไม่มีวันหมดอายุ ใช้ลดราคา

**ต้องมีทั้งคู่ ห้ามยุบเป็นอันเดียว** — ทางที่ถูกคือใช้ตาราง `members` ร่วมกัน
แล้วให้ข้อมูลแพ็ก/วันหมดอายุเป็นส่วนเสริมของยิมโดยเฉพาะ

### 3. แอดออนคนละวิธีขาย
- ยิม: ขายแยกชิ้น — KDS ฿290 · จอคิว ฿190 · สมาชิกสั่งอาหาร ฿290
- ร้านอาหาร: ขายเป็นแพ็กเกจรวม ฿299 / ฿599

---

## ⬆️ ของที่ฟิตเนสมี แต่ร้านอาหารไม่มี — ควรยกขึ้นเป็นของกลาง

| ของ | ทำไมมีค่า |
|---|---|
| **Voucher / บัตรของขวัญ** | มีโค้ด · ยอดคงเหลือ (ใช้ทีละส่วนได้) · วันหมดอายุ · ขั้นต่ำ · เพดานลด · จำกัดจำนวนสิทธิ์ — ร้านอาหารมีแค่โปร ไม่มีบัตรของขวัญเลย |
| **สต็อกผูกในแพ็ก** | รู้ว่าของกี่ชิ้นติดค้างลูกค้าอยู่จากแพ็กที่ขายไปแล้ว → เตือนให้ซื้อเข้าก่อนของขาด |
| **บัตรสมาชิกโค้ดหมุน 30 วินาที** | กันคนแคปหน้าจอบัตรไปใช้ต่อ — ใช้กับบัตรสะสมแต้มร้านอาหารได้เลย |
| **ต้นทุนเฉลี่ยถ่วงน้ำหนักตอนซื้อเข้า** | คิดทุนใหม่ทุกครั้งที่รับของ |

## ⬇️ ของที่ร้านอาหารมี แต่ฟิตเนสไม่มี

ขายฝากเต็มระบบ · จับคู่สลิปอัตโนมัติผ่าน LINE (ฟิตเนสต้องวางข้อความเอง) ·
โปรที่คิดยอดฝั่งเซิร์ฟเวอร์ (ฟิตเนสคิดในเบราว์เซอร์ = แก้ยอดได้) · รูปขึ้นที่เก็บไฟล์จริง ·
Mobile order ตัวจริง · กระเป๋าเงิน/เติมเงิน · รายงานที่ดึงจากฐานข้อมูล

---

## แผน 4 เฟส

### เฟส 0 — ใช้งานได้ทันที ไม่ต้องเขียนโค้ด
ยิมที่มีคาเฟ่เปิดร้านใน POS หลักไปเลยสำหรับขายของ · ใช้แอปฟิตเนสสำหรับสมาชิก/คลาส
**ข้อเสีย**: ข้อมูลอยู่ 2 ที่ · **ข้อดี**: ได้ขายฝาก/โปร/Mobile order วันนี้เลย

### เฟส 1 — รวมตัวตน (เล็ก)
ยิม 1 แห่ง = ร้าน 1 ร้าน ใช้รหัสร้านเดียวกัน · `gym.id` ↔ `shops.id`
ทำให้ข้อมูลไม่แตกเป็นสองก้อนตั้งแต่ต้น **ควรทำก่อนมียิมจริงใช้งาน**

### ✅ เฟส 2 — ยกของกลางขึ้นก่อน (เสร็จแล้ว 4 ส.ค. 2569)
เอา **Voucher/บัตรของขวัญ** ขึ้นเป็นระบบกลางใน `kaidee-pos-worker`
→ ร้านอาหารได้ของใหม่ · ฟิตเนสได้เวอร์ชันที่คิดยอดฝั่งเซิร์ฟเวอร์
**ทำแล้ว** — ตาราง `voucher_defs` + `vouchers` ใน `kaidee-pos-worker` · ร้านตั้งแม่แบบและออกบัตรได้จาก
ตั้งค่าร้าน → บัตรกำนัล & คูปอง · ลูกค้ากรอกโค้ดในช่องเดิม (ไม่ต้องรู้ว่าเป็นโปรหรือบัตร ระบบลองให้เอง)
ตรวจ/ตัดยอด/ตัดสิทธิ์ที่เซิร์ฟเวอร์ทั้งหมด ต่างจากของเดิมฝั่งฟิตเนสที่คิดในเบราว์เซอร์

**ยังเหลือ**: ฝั่งฟิตเนสยังใช้ voucher ของตัวเองอยู่ ต้องเปลี่ยนไปเรียกของกลาง — ทำพร้อมเฟส 3

### เฟส 3 — ย้ายระบบขาย/สต็อกไปใช้ของกลาง (ใหญ่)
`products→menu` · `raws→raw` · `purchases` · `orders` · `renewals(shop)→sales` · `dayCloses→cash_days`
หน้าจอฟิตเนสยังหน้าตาเดิม แค่ข้างในเรียกฐานกลาง
**เสี่ยงสุด** — ต้องมีตัวย้ายข้อมูลของยิมที่ใช้อยู่แล้ว และทดสอบว่ายอดขายเก่าไม่หาย

### เฟส 4 — รวมหน้าจอ (ใหญ่)
คลาส/เช็คอิน/แพ็กเกจ เข้ามาเป็นแท็บในแอปหลัก เลือกแสดงตามประเภทร้าน

---

## ประเมินแรง (คร่าว ๆ)

| เฟส | แรง | ความเสี่ยง |
|---|---|---|
| 0 | ไม่มี | ไม่มี |
| 1 | ~ครึ่งวัน | ต่ำ |
| 2 | ✅ เสร็จแล้ว | — |
| 3 | **หลายวัน** | **สูง — แตะเงินและสต็อกของยิมที่ใช้จริง** |
| 4 | หลายวัน | กลาง |

## ที่แนะนำ
~~เฟส 0 → 1 → 2~~ — **เฟส 2 เสร็จแล้ว** เหลือเฟส 1 (รวมตัวตน) ซึ่งควรทำก่อนมียิมจริง
**เฟส 3 อย่าเพิ่งทำจนกว่าจะมียิมจริงใช้งาน** — ตอนนี้ยังไม่มีข้อมูลจริงให้เสี่ยง
และถ้าย้ายตอนไม่มีผู้ใช้ ก็ยังต้องมาย้ายซ้ำอยู่ดีเมื่อของจริงเข้ามา

---

## หมายเหตุ
- ตัวเลขความยาวไฟล์: `fitness-pos.jsx` 247KB แต่ 1,794 บรรทัด (ภาษาไทยกิน 3 ไบต์ต่อตัว)
- `gate.js` ปิดตัวเองบนโฮสต์จริง เป็นด่านสำหรับพรีวิวเท่านั้น — แต่รหัสฝังอยู่ในไฟล์ที่อยู่ใน repo สาธารณะ ควรย้ายไปตั้งจากภายนอก (ไม่เร่งด่วน)
