name = "platform"
main = "worker.js"
compatibility_date = "2025-01-01"

# ── D1 database ──
# 1) npx wrangler d1 create platform   → คัดลอก database_id ที่ได้มาใส่ตรงนี้
[[d1_databases]]
binding = "DB"
database_name = "platform"
database_id = "9aa67698-cab2-433a-b94f-dcbd593319ba"

# ── Durable Object (realtime WebSocket) — ไม่ผูกก็ deploy ได้ แต่ /ws จะคืน 501 (client fallback poll) ──
[[durable_objects.bindings]]
name = "BIZ_HUB"
class_name = "BizHub"

# ใช้ new_sqlite_classes รองรับ Free plan (BizHub ไม่ใช้ storage แต่ migration ต้องประกาศ)
[[migrations]]
tag = "v1"
new_sqlite_classes = ["BizHub"]

# ── ตัวแปร / secret (ตั้งเพิ่มได้ · ไม่ตั้งก็ทำงาน) ──
# [vars]
# ADMIN_PASS = "platform2026"     # รหัสแอดมิน (default platform2026) — แนะนำตั้งเป็น secret แทน
#
# ตั้ง secret (ปลอดภัยกว่า vars):
#   npx wrangler secret put ADMIN_SECRET   # กุญแจเซ็น token (สุ่มยาว ๆ)
#   npx wrangler secret put ADMIN_PASS     # รหัสเข้า Back Office
#   npx wrangler secret put LINE_TOKEN     # (ถ้าจะยิง LINE push)
#   npx wrangler secret put VAPID_PRIVATE_JWK   # คีย์ลับสำหรับแจ้งเตือนแอปไรเดอร์
#
# แจ้งเตือนงานใหม่: ใช้ Web Push (ฟรีไม่จำกัด) เป็นหลัก
# ถ้าอยากยิง LINE ด้วย ให้ตั้ง NOTIFY_VIA_LINE = "1" — แต่ LINE OA คิดเงินตามจำนวน
# ข้อความ ฟรีแค่ 500/เดือน ไรเดอร์ 10 คนวันละ 20 งานก็ 6,000 ข้อความแล้ว

[vars]
# คีย์สาธารณะของระบบแจ้งเตือน — ไม่ลับ ฝั่งแอปต้องใช้ตัวนี้ตอนขอสิทธิ์
VAPID_PUBLIC = "BHTi8CnozRn2kICdws47K2fiRj_hD0T8mUuZAO9tKav8-MrGPnn-obkjwj9kEi9FOs2eGtJF0C44zG8Qg4-CUhw"
