# KaiDee POS (":Done KaiDee POS") — SESSION HANDOFF

แอป POS ผ่าน LINE สำหรับร้านอาหาร + ระบบตลาด/แรงงาน (Labor Win) ในเครือเดียวกัน
Deploy: Cloudflare Workers (git-integrated, auto-deploy on push to `main`)
- Frontend: `kaidee-git` worker (serves `./kaidee-deploy`) — auto-deploy ผ่าน Cloudflare's native git integration
- Backend: `kaidee-pos-worker/`, `license-worker/`, `platform-worker/` — auto-deploy ผ่าน `.github/workflows/deploy-worker.yml` (GitHub Actions)

---

# 📌 อ่านตรงนี้ก่อน (สถานะล่าสุด 2026-08-03)

## 🔑 ต้องตั้ง secret ก่อน แจ้งเตือนไรเดอร์ถึงจะทำงาน
ตั้ง `VAPID_PRIVATE_JWK` ที่ Cloudflare → Workers → **platform** → Settings → Variables and Secrets
ค่าคีย์ลับ **ไม่เก็บในไฟล์นี้และห้ามเก็บใน repo** (repo นี้ public) — เจ้าของระบบเก็บไว้เอง
ถ้าหาย: สร้างคู่ใหม่ได้ตลอด แล้วเอาคีย์สาธารณะไปแทนใน `platform-worker/wrangler.toml` (`VAPID_PUBLIC`)
```bash
node -e 'const{generateKeyPairSync}=require("crypto");const{publicKey,privateKey}=generateKeyPairSync("ec",{namedCurve:"prime256v1"});const p=publicKey.export({format:"jwk"}),d=privateKey.export({format:"jwk"});console.log("PUBLIC :",Buffer.concat([Buffer.from([4]),Buffer.from(p.x,"base64url"),Buffer.from(p.y,"base64url")]).toString("base64url"));console.log("PRIVATE:",JSON.stringify({kty:d.kty,crv:d.crv,x:d.x,y:d.y,d:d.d}))'
```
สร้างใหม่แล้วไรเดอร์ที่เคยกดเปิดแจ้งเตือนต้องกดเปิดใหม่ทุกคน (ทะเบียนเดิมใช้ไม่ได้กับคีย์ใหม่)
เช็คว่าตั้งสำเร็จ: `curl "https://platform.oneday-pos.workers.dev/pool/push/key?region=delivery"` ต้องได้ `enabled: true`

> ⚠️ คีย์ชุดแรกที่สร้างไว้ (ขึ้นต้น `BK4T5…`) **เคยหลุดอยู่ในประวัติ git ของ repo public นี้** — เปลี่ยนเป็นชุดใหม่แล้ว ห้ามนำชุดเก่ากลับมาใช้

## 💸 เรื่องค่าใช้จ่ายที่ต้องรู้
- **LINE OA ฟรีแค่ 500 ข้อความ/เดือน** — แจ้งเตือนไรเดอร์จึงเปลี่ยนไปใช้ Web Push (ฟรีไม่จำกัด) แล้ว
- ฝั่งลูกค้ายังยิง LINE อยู่ ~3 ข้อความ/ออเดอร์ (100 ออเดอร์/วัน ≈ 9,000/เดือน) — ปรับได้ด้วย `NOTIFY_LINE_LEVEL` บน kaidee-pos-worker: `all` ทุกสถานะ · `key` เฉพาะสำคัญ (ค่าเริ่มต้น) · `off` ปิด
- ฝั่งร้านไม่มีค่าใช้จ่ายต่อออเดอร์ (ร้านไม่ได้รับ LINE) แต่ยังต้องเปิดแอปค้างถึงจะรู้ว่ามีออเดอร์ใหม่ — **ยังไม่ได้ทำ Web Push ฝั่งร้าน**


## ลิงก์ที่ใช้งานได้จริงตอนนี้
| ใคร | ลิงก์ |
|---|---|
| หน้าสมัคร (หน้าแรกของเว็บ) | `https://kaidee-git.oneday-pos.workers.dev/` → เด้งไป `Signup Chooser.html` |
| ลูกค้า — ร้านทั้งหมด (แบบ Grab) | `.../?role=market` · เจาะตลาดเดียว `&market=<ชื่อตลาด>` |
| ลูกค้า — สั่งจากร้านหนึ่ง | `.../?shop=<รหัสร้าน>&role=customer` (ถ้ามาจากหน้าแพลตฟอร์มจะมี `&via=market`) |
| ร้านค้า (หลังร้าน) | `.../index.html` หรือ `.../?shop=<รหัสร้าน>&role=merchant` |
| สมัครไรเดอร์แพลตฟอร์ม | `.../Rider%20Signup.html` |
| แอปไรเดอร์ | `.../Rider%20App.html` (ติดตั้งเป็นแอปบนเครื่องได้) |
| ระบบวิน (Labor Win) | `.../Labor%20Win%20App%20v2.html?role=<shop\|worker\|win\|market>` |
| Back Office แอดมิน | `.../admin/` (แท็บ "🛵 ใบสมัครไรเดอร์" ใช้รหัสแอดมินของ **platform-worker** แยกจาก POS) |

## 3 ระบบไรเดอร์ที่ห้ามสับสน
1. **ไรเดอร์แพลตฟอร์ม** (ตัวหลัก) — `Rider Signup.html` + `Rider App.html` → platform-worker `coll='riders'`, งานอยู่ `region=delivery`
2. **ระบบวิน (Labor Win)** — คนละชุด มีหัวคิว/ค่าคอม 2% → `region=pool`
3. **ไรเดอร์ประจำร้าน** — `kaidee-rider.jsx` (`?shop=x&role=rider`) · **ฟอร์มสมัครเป็นของจำลอง ไม่ต่อหลังบ้าน** ยังไม่ตัดสินใจว่าจะทำยังไงต่อ

## ⏳ งานค้าง (เรียงตามความสำคัญ)
1. **ตั้ง `VAPID_PRIVATE_JWK`** (ดูหัวข้อบนสุด) — ไม่ตั้ง = ไรเดอร์ไม่ได้รับแจ้งเตือนเลย
2. **รหัสผ่านแอดมิน `kaidee-pos-worker`** อาจยังเป็น default `kaidee2026` ที่ public บน GitHub — **เจ้าของระบบต้องเปลี่ยนเอง**
3. **Web Push ฝั่งร้าน** — ร้านยังต้องเปิดแอปค้างถึงจะรู้ว่ามีออเดอร์ใหม่ · กลไกเดียวกับฝั่งไรเดอร์ ยกมาใช้ซ้ำได้เลย (`rider-sw.js` + `/pool/push/*`)
4. **build step (esbuild)** — เปิดแอปครั้งแรกยังใช้เวลา ~6.8 วิ เพราะ `babel.min.js` 3MB แปลง JSX ในเบราว์เซอร์ตอนรัน — **เจ้าของระบบเลือกว่ายังไม่ทำ (2026-08-03)**
5. **ฟอร์มสมัครไรเดอร์ในแอปร้าน** (`kaidee-rider.jsx`) — **ตัดสินใจแล้ว: เปลี่ยนเป็นลิงก์ไปหน้าสมัครแพลตฟอร์ม** ยังไม่ได้ลงมือ
6. **นโยบายเก็บ/ลบเอกสาร KYC ไรเดอร์** (รูปบัตร ปชช./ใบขับขี่ เก็บเป็น data URL ใน D1) — ยังไม่มีอายุการเก็บ เป็นประเด็น PDPA เมื่อมีผู้สมัครจริง
7. **รอบส่ง (รวมออเดอร์หลายแผงจ่ายค่าส่งรอบเดียว) + Personal Shopper** — คุยไว้ละเอียดแล้ว ยังไม่มีโค้ด · จุดตัดสินใจที่ค้าง: ค่าส่งบอกลูกค้ายังไงตอนยังไม่รู้ว่ารอบนี้มีกี่คน (เสนอ: คงที่ ฿15/คน) และคนช่วยซื้อจะไม่ควักเงินเอง (เสนอ: จ่ายรายแผงตอนชั่งเสร็จผ่าน QR ของแผงนั้น)
8. **แชทบอทตอบลูกค้าทาง LINE** — ออกแบบ 4 ชั้นไว้แล้ว ยังไม่มีโค้ด
9. **Timeout ปล่อยงานอัตโนมัติ + สถิติไรเดอร์ทิ้งงาน** — รอมีปริมาณงานจริง
10. **คู่มือในแอปส่วน "ไรเดอร์ร้าน"** ยังอธิบายระบบเก่า (ผูกกับข้อ 5)
11. **LINE OA**: rich menu 3 ปุ่ม + ข้อความทักทาย — ยังไม่ยืนยันว่าตั้งค่าจริงหรือยัง
12. **ร้านโปรดยังผูกกับเครื่อง** ไม่ใช่บัญชี LINE — ย้ายเครื่องแล้วหาย ต้องมีตารางใหม่ฝั่งเซิร์ฟเวอร์ถึงจะแก้ได้

## ⚠️ นิสัยที่ต้องมีเวลาทำงานกับระบบนี้
- **ฝั่งแอปกลืน error API ทุกจุด** (`.catch(()=>{})`) — บั๊กพังเงียบได้เป็นเดือน (เคยเกิดจริงกับ `POST /menu`) **ต้องยิง curl ทดสอบ endpoint จริงเสมอ อย่าเชื่อว่า "โค้ดมี = ทำงาน"**
- **ทดสอบด้วยเบราว์เซอร์จริงด้วย ไม่ใช่แค่ API** — บั๊ก `disabled:false` ที่ทำให้ปุ่มทุกปุ่มในแอปไรเดอร์กดไม่ได้ ตรวจจาก API ไม่มีทางเจอ
- **root กับ `kaidee-deploy/` ต้อง sync มือทุกครั้ง** ที่แก้ frontend + bump `?v=` ใน `index.html` ไม่งั้นเครื่องเก่าไม่ได้โค้ดใหม่
- **`kaidee-pos-worker` / `platform-worker` / `license-worker` คนละ D1 คนละ deploy คนละรหัสแอดมิน**
- worker deploy ผ่าน GitHub Actions (เช็ค `gh run list --workflow=deploy-worker.yml`) · frontend deploy ผ่าน Cloudflare git integration (ช้ากว่า ~1-2 นาที)

---

## 🗓️ Session 2026-08-01 (คืนยาว — audit เต็มระบบ + business strategy + Labor Win)

### ✅ บั๊กที่แก้ + deploy แล้ว (kaidee-pos-worker)
1. `POST /consignment/delivery-note/:id/confirm` ถูก route อื่น (`delivery-note` ไม่มี `seg[2]` guard) บังจนใช้งานไม่ได้ — แก้แล้ว
2. `.github/workflows/deploy-worker.yml` พังทั้งไฟล์ — `hashFiles()` อยู่ผิดที่ (job-level `if:` ไม่รองรับ, ต้องอยู่ใน `steps.*` เท่านั้น) → **deploy พังมาตั้งแต่ก่อน session นี้ อาจทำให้งานก่อนหน้าไม่เคยขึ้น production จริง** — แก้แล้ว, CI เขียวปกติแล้ว
3. `pay_requests` table ขาดคอลัมน์ `handled_at`/`handled_by` ที่โค้ด PATCH ใช้อยู่แล้ว → 500 ทุกครั้งที่แอดมินกดอนุมัติ/ปฏิเสธคำขอชำระเงิน — แก้แล้ว (เพิ่ม ALTER ใน `ensurePayReqCols`)
4. `POST /line-webhook` **ใช้งานไม่ได้เลยตั้งแต่แรก** — ติด global gate `if (!shop) return err(...)` เพราะ LINE เรียก URL นี้จริงไม่มี `?shop=` มาด้วย (ต้อง resolve เองผ่าน group→shop mapping) → ย้าย handler ไปอยู่ก่อน gate แล้ว — ยืนยันแล้วว่า group pairing + bank-alert auto-match ทำงานจริง
5. `DELETE /shops/:id` ลบข้อมูลไม่ครบ — เหลือ wallet/vendors/consignment/inv-tx/refunds/code-requests/line-pairs/admin-log ค้าง (ปัญหา PDPA) — แก้แล้ว ลบครบทุกตารางที่เกี่ยวข้อง (wallet ใช้ `biz_id` แยกจาก `shop_id`)
6. Paywall (`freeTier` gate ใน `kaidee-merchant2.jsx`) เชื่อ `sub.plan`/`sub.expiry` ที่มาจาก localStorage เป็นหลัก — แก้ localStorage เองข้ามล็อกได้ → เปลี่ยนให้เชื่อ `sub._licActive` (ค่าจากเซิร์ฟเวอร์) ก่อนเสมอเมื่อเช็คสำเร็จแล้ว, fallback local date-math เฉพาะก่อนเช็คครั้งแรก/ออฟไลน์

### 🚨 ช่องโหว่ความปลอดภัยวิกฤต — แก้แล้วทั้ง 2 worker
**Pattern**: endpoint แอดมินเช็คแบบ `if (env.ADMIN_SECRET && !verifyToken(...))` — ถ้า `ADMIN_SECRET` ไม่ถูกตั้งค่า เงื่อนไขทั้งก้อน short-circuit เป็น false → **ข้ามการเช็คสิทธิ์ทั้งหมด ไม่ต้องมี token ก็เรียกได้**
- **`kaidee-pos-worker`**: ยืนยันแล้วว่า exploitable จริง (ดึงข้อมูล wallet จริงได้แบบไม่ auth ก่อนแก้) → ตั้ง `ADMIN_SECRET` ใหม่แล้ว (เก็บนอกไฟล์นี้ — ไม่ใช่รหัสผ่าน login เป็นแค่ signing key)
- **`platform-worker`**: ตั้ง `ADMIN_SECRET` + `ADMIN_PASS` ใหม่แล้วเช่นกัน (พบตอน deploy คืนนี้ ก่อนหน้านี้ทั้งสอง secret ไม่เคยถูกตั้งเลย ใช้ fallback ที่ public บน GitHub)
- เพิ่ม **rate limit login แอดมิน** (`kaidee-pos-worker`): ผิด 5 ครั้ง ล็อก IP 15 นาที (ก่อนหน้านี้ brute-force ได้ไม่จำกัด)

**⏳ ยังไม่ได้ทำ (ต้องเจ้าของระบบทำเอง)**:
- เปลี่ยนรหัสผ่าน login แอดมิน `kaidee-pos-worker` (อาจยังเป็นค่า default `kaidee2026` ที่ public บน GitHub) — ต้องผ่าน Back Office + OTP มือถือ/LINE เจ้าของ
- เช็คว่าช่องโหว่เคยถูกใช้จริงก่อนคืนนี้ไหม (ดู `admin_access_log`, ยอด wallet ทุกร้าน, เลขบัญชีกลาง `wallet-account`)
- **รหัสผ่านแอดมิน `platform-worker` ใหม่**: `e5421493868dfe121a99f5587fbdee51` (ให้ผู้ใช้ไปแล้วในแชท ควรเปลี่ยนเองผ่าน `/admin/password` เมื่อสะดวก)

### 🐛 บั๊กเล็กที่แก้ (platform-worker, เจอโดย agent test)
`GET /pool/jobs/near` และ `/pool/workers/near` — ถ้าไม่ส่ง `lat`/`lng` มา `+null` กลายเป็น `0` (ไม่ใช่ `NaN`) ทำให้กรองระยะทางเทียบกับพิกัด (0,0) แทนที่จะไม่กรองเลย → แก้แล้ว (เช็ค presence ของ param ก่อน)

### ✅ ฟีเจอร์ใหม่ + deploy แล้ว
1. **Auto-verify wallet topup** (`kaidee-pos-worker`) — จับคู่ข้อความแจ้งเงินเข้าบัญชีกลาง (ส่งผ่าน `POST /wallet-bank-alert`, admin-gated) กับ topup ที่ pending อยู่ข้ามทุกร้าน ยืนยันอัตโนมัติถ้ายอดตรง (tolerance 0.5, FIFO ถ้ายอดชนกัน) — ลดงานแอดมินตรวจสลิปมือ
2. **MarketHome** — หน้า "ร้านในตลาดนี้" ฝั่งลูกค้า (`kaidee-market-home.jsx`, ใหม่) เข้าทาง `?role=market&market=<ชื่อตลาด>` (ไม่บังคับ LINE login, เหมือน board/kds) แสดงร้านในตลาดเดียวกัน (กรองจาก field `market` ใหม่ใน `shops.extra` JSON) กดร้าน → navigate เต็มหน้าเข้า flow สั่งอาหารเดิม backend: `GET /shops/by-market?market=`, ตั้งค่าผ่าน `PATCH /shops/:id {market:'...'}`
   - **ตั้งใจให้เป็น "ตลาดเดียวต่อลิงก์" ไม่ใช่มาร์เก็ตเพลสรวมทั้งเมืองแบบ Grab** — แต่ละตลาดมี query param `market=` แยกกัน สร้างกี่ตลาดก็ได้แค่เปลี่ยนชื่อ ไม่ต้องเพิ่มโค้ด
   - โหมดปัจจุบัน = สั่ง/จองล่วงหน้า → มารับเอง เท่านั้น (ยังไม่มีเดลิเวอรี/ไรเดอร์เชื่อมเข้ามา)
3. **platform-worker deploy จริงครั้งแรก** — `wrangler.toml` เคยมี `database_id` เป็น placeholder ทำให้ CI deploy ล้มเหลวตลอด (เคยเอา job ออกจาก CI ไปก่อนตอนต้นคืน) → เจอ D1 database "platform" ที่เคยสร้างไว้แล้ว (uuid `9aa67698-cab2-433a-b94f-dcbd593319ba`) ใส่กลับเข้า config, apply schema, deploy จริง, เพิ่ม CI job กลับเข้าไป — **ระบบ Market/Pool (Labor Win) ใช้งานได้จริงแล้ว ไม่ใช่แค่โค้ดเฉยๆ**

### 🎪 Demo data สร้างไว้ใน production (kaidee-pos-worker) — ใช้พรีเซ้นต์ร้านค้าได้เลย
4 ตลาดสาธิต พร้อมร้าน+เมนูจริง ทดสอบ end-to-end แล้ว:
- `ตลาดสาธิต (Demo)` — 4 ร้าน (ส้มตำ/ก๋วยเตี๋ยว/น้ำปั่น/ขนมหวาน)
- `ตลาดลาดสวาย` — 3 ร้าน (ผัดไทย/ลาบก้อย/กาแฟโบราณ)
- `ตลาดสี่มุมเมือง` — 3 ร้าน **ราคาส่ง B2B** (ผัก/ผลไม้/เนื้อสัตว์ ขายเป็นกิโล) — ไอเดีย: แม่ค้าตลาดลาดสวายสั่งวัตถุดิบจากที่นี่ผ่านแอปเดียวกับที่ขายลูกค้าตัวเอง
- `ตลาดนานาเพลส` — 3 ร้าน (ซูชิ/สลัด/เบเกอรี่)

ลิงก์ (เจาะจงตลาดเดียว): `https://kaidee-git.oneday-pos.workers.dev/?role=market&market=<ชื่อตลาด URL-encoded>`

ลิงก์ **"ร้านทั้งหมด" แบบ Grab (รวมทุกตลาดในฟีดเดียว, ทำเสร็จช่วงบ่าย/เย็น 2026-08-01)**: `https://kaidee-git.oneday-pos.workers.dev/?role=market` (ไม่ใส่ `market=`) — โชว์ร้านจาก 4 ตลาดสาธิตด้านบนรวมกัน แต่ละการ์ดมี badge 📍ชื่อตลาด บอกว่าร้านอยู่ตลาดไหน ใช้ตัวนี้เป็นลิงก์หลักตอนพรีเซนต์ภาพรวมทั้งแพลตฟอร์ม ส่วนลิงก์เจาะจงตลาดด้านบนไว้ใช้ตอนโฟกัสตลาดเดียว

Labor Win demo (platform-worker, ใช้งานได้จริงแล้วหลัง deploy คืนนี้): `https://kaidee-git.oneday-pos.workers.dev/Labor%20Win%20App%20v2.html?role=<market|shop|worker|win>`

---

## 💡 การตัดสินใจเชิงกลยุทธ์ (คุยคืนนี้ ยังไม่ได้ลงมือ เว้นที่ระบุว่าทำแล้ว)

### โมเดลธุรกิจ — ยืนยันแล้ว
- **Zero-GP**: ร้านไม่โดนหักค่าคอมมิชชั่นต่อออเดอร์ (ต่างจาก Grab ที่หัก 20-35% + บังคับร้านบวกราคาหน้าร้าน) รายได้ KaiDee มาจากค่าสมัครรายเดือน (฿299 "ร้าน" / ฿599 "โปร") ทดลองฟรี 30 วัน
- **ไม่ต้องซื้อเครื่อง Sunmi** (เป็น PWA เปิดผ่าน LINE, ไม่ต้อง install แอปแยก — ต่างจาก Grab/Wongnai ที่ต้องมีแท็บเล็ต)
- **กลยุทธ์เจาะตลาดเดียวก่อน (beachhead)**: ห้ามทำมาร์เก็ตเพลสรวมทั้งเมืองตอนนี้ (จะกลายเป็นหน้าว่างเปล่า) — เลือก 1 ตลาดที่มีคนรู้จัก onboard มือ (white-glove) 10-20 ร้านแรกก่อน ค่อยขยาย

### Go-to-market
- Pitch หลัก: "ราคาเท่าตลาด ไม่บวกเพิ่ม" — สู้ Grab ไม่ได้เรื่องส่วนลดหวือหวา (Grab subsidize หนัก) แต่สู้ได้เรื่องราคาจริง/กำไรเต็มร้าน สำหรับกลุ่มลูกค้าประจำที่ไว้ใจร้านอยู่แล้ว (ไม่ใช่นักล่าดีล)
- ร้านมีพื้นที่ทำโปรเองได้มากกว่าร้านบน Grab (เพราะไม่มี GP กินกำไรไปก่อน) — ใช้จุดนี้ชวนแบรนด์มีชื่อที่ margin ดีอยู่แล้วได้ด้วย
- เฟส 1 ให้ KaiDee ออกค่าคูปอง/ส่วนลดเองก่อน (จากงบการตลาด) ไม่รอสปอนเซอร์จริง ไม่เก็บร้าน — ใช้กลไก `Sponsor Console.html` ที่มี prototype อยู่แล้ว (ยังไม่ได้ integrate)
- ไอเดียต่อยอด B2B: ตลาดสี่มุมเมือง (ค้าส่ง) ↔ ตลาดลาดสวาย (ค้าปลีก) แม่ค้าปลีกสั่งวัตถุดิบผ่านแอปเดียวกับที่ขายลูกค้า — ยังไม่ทำ แค่สร้าง demo data ไว้

### ระบบไรเดอร์/แรงงาน — decision สำคัญคืนนี้
- **ทางเลือก A (ตัดสินใจแล้ว ใช้ทางนี้)**: ลูกค้าจ่ายค่าอาหารตรงร้าน (PromptPay) + จ่ายค่าไรเดอร์ตรงไรเดอร์ (สด/โอน) — **แพลตฟอร์มไม่แตะเงินเลยในเฟสแรก** หลีกเลี่ยงประเด็นกฎหมาย e-money ของ ธปท.
- **ไรเดอร์สมัครอิสระ ไม่ผ่านหัวหน้าวิน** — ตัดสินใจแล้วว่าไม่ใช้โมเดลหักค่าคอมให้หัวหน้าวิน (ขัดกับหลัก zero-GP) ใช้ระบบ Worker Pool ที่มีอยู่แล้ว (`platform-worker` `/pool/*`) เป็นทางหลัก — สมัครเองได้ทันที ไม่ต้องรอใครอนุมัติ
- **ป้องกันไรเดอร์ทิ้งงาน**: เฟสนี้ (เพิ่งเริ่ม) แก้ด้วยคนก่อน — คัดไรเดอร์ที่รู้จัก ไม่เปิดสมัครอิสระวงกว้าง พอมีปริมาณงานจริงค่อยเพิ่ม timeout auto-release + strike counter (ยังไม่ได้สร้าง)
- **ช่องว่างเรื่องตรวจเอกสาร (KYC)**: ระบบ Pool ตอนนี้สมัครได้ทันทีไม่มีการตรวจตัวตนเลย (ต่างจาก Grab ที่บังคับส่งบัตร ปชช./ใบขับขี่ก่อน) — มีไฟล์ `kaidee-rider.jsx` ที่มีแนวคิดตรวจเอกสารอยู่แล้วแต่ไม่ได้เชื่อมกับ Pool — ยังไม่ได้ทำ, เป็นความเสี่ยงด้านความปลอดภัยที่ต้องคิดต่อ

### ระบบวิน (Labor Win) — ข้อค้นพบ
- โครงสร้าง 3 สถานะ: สังกัดวินอย่างเดียว / สังกัดวิน+รับงานนอก / ฟรีแลนซ์ล้วน — ของเดิมมีแนวคิด "งานนอกหัวหน้าวินไม่เห็นรายละเอียด" (ป้องกัน worker แต่ hidden ค่าคอมยังหักอัตโนมัติ 2%)
- ถกกันยาวเรื่อง "หัวหน้าวินควรเห็นงานนอกไหม" — **สรุปสุดท้าย: ไม่ใช้ระบบวินเป็นทางหลักแล้ว** (ดูหัวข้อไรเดอร์ด้านบน) ประเด็นนี้เลยไม่ต้อง action ต่อ
- ระบบรองรับ **bottom-up onboarding อยู่แล้ว**: ใครก็สร้างตลาดเองได้ (`POST /biz`, ไม่ต้องมีเจ้าของตลาดจริงมาก่อน) ออกโค้ดเชิญได้เอง, ร้านค้าเพิ่มตลาดใหม่ได้เองจากหน้า shop ถ้ายังไม่เจอในลิสต์ (sync ผ่าน shared state key `demo-laborwin2` by default หรือ `?biz=` กำหนดเอง)
- **หมายเหตุความปลอดภัย**: `POST /biz` ไม่มีการล็อกสิทธิ์เลย ใครก็สร้าง/อ้างสิทธิ์ตลาดได้ — ยอมรับได้สำหรับตอนนี้ (เราใช้เอง) แต่ถ้าเปิดสาธารณะจริงในอนาคตต้องเพิ่มการยืนยันตัวตน

### ยังไม่ได้ทำ (ของที่คุยไว้เป็นไอเดีย ยังไม่แตะโค้ด)
1. **แชทบอทตอบลูกค้าทาง LINE** — มีดีไซน์ 4 ชั้นแล้ว (rich menu ปุ่มลัด → บอทจับคำสำคัญใน `line-webhook` → เปิดเคสหาแอดมิน (pattern เดียวกับ pay-requests) → เช็คสถานะเคส) ยังไม่ได้สร้างโค้ดสักบรรทัด
2. **ป้ายโปรบนการ์ดร้าน** ใน MarketHome — เสนอเป็นขั้นต่อไปที่ต่อยอดง่ายจาก `market` field ที่มีอยู่ ยังไม่ทำ
3. **Backend เชื่อมเมนูข้ามสาขา** (แบรนด์หลายสาขาอัปเดตเมนูครั้งเดียว sync ทุกสาขา) — ตัดสินใจแล้วว่ายังไม่ทำ รอมีแบรนด์จริงมาขอก่อน
4. **Timeout auto-release + strike counter** สำหรับไรเดอร์ทิ้งงาน — ยังไม่ทำ รอมีปริมาณงานจริงก่อน
5. **KYC/ตรวจเอกสารไรเดอร์** ก่อนเข้าระบบ Pool — ยังไม่เชื่อมกับ `kaidee-rider.jsx` ที่มีแนวคิดอยู่แล้ว

---

## 🗓️ Session 2026-08-01 ภาคบ่าย/เย็น (ต่อจากคืนก่อน — สลับบัญชี Claude, งานส่งต่อ)

### ✅ ทำเสร็จ + commit/push แล้ว (เช็คแล้วว่าขึ้น GitHub จริง ปลอดภัย)
1. **เปลี่ยนชื่อแอป** `:Done KaiDee POS` → **`:Done KaiDee`** (ตัด "POS" ออก) — `index.html`, `manifest.json` ทั้ง root/`kaidee-deploy` (commit `172b25d`)
2. **Rebrand สีทั้งแอปจากเขียว-มรกตเป็นน้ำเงิน `#26619C`** (ผู้ใช้เลือกเอง หลังดู mockup จริงหลายรอบ) — แก้ครบ `index.html`, `kaidee-app/crm/home/merchant2/quote.jsx`, `backoffice-app.jsx`, `shop-backoffice.html`, `KaiDee Back Office.html`, `manifest.json` (theme_color) ทั้ง root/`kaidee-deploy` (commit `5ebeee2`)
   - **ตั้งใจไม่แตะสีสถานะ (semantic)**: badge "รับแล้ว/OK ✓" ในรายงาน, ตัวเลข +/- ใน wallet ledger, สีตามหมวดธุรกรรม (`gp_remit`/`labor`/`topup` ใน `backoffice-app.jsx`) — ยังเขียว/สีเดิมตามหน้าที่ ไม่ใช่สีแบรนด์
   - พบไฟล์ `kaidee-market-home.jsx` มี fallback hex เขียวหลงเหลือ (`var(--brand,#0E9C88)` ฯลฯ) ที่พลาดไปตอนแรก — แก้เพิ่มพร้อมงานข้อ 4 ด้านล่างแล้ว
3. **ฟีเจอร์ "ร้านทั้งหมด" แบบ Grab (customer directory)** — ตัดสินใจฉีกจากกลยุทธ์ beachhead เดิม (ที่บอกว่า "ห้ามทำมาร์เก็ตเพลสรวมเมือง") เพราะอยากให้แพลตฟอร์มถูกรู้จักวงกว้างขึ้นด้วย ไม่ใช่แค่ตลาดเดียว:
   - Backend: `GET /shops/directory` ใหม่ใน `kaidee-pos-worker/worker.js` — คืนร้าน active ทุกร้านที่ตั้ง `market` ไว้แล้ว จากทุกตลาดรวมกัน เรียงตามชื่อตลาด
   - Frontend: `kaidee-market-home.jsx` — ตอนนี้ `?role=market` **ไม่ใส่** `market=` พารามิเตอร์ จะโชว์ฟีดรวมทุกร้านแทนที่จะขึ้น error "ไม่ได้ระบุตลาด" แต่ละการ์ดร้านมี **badge 📍ชื่อตลาด** ติดไว้ (สีฟ้า accent แยกจาก badge เปิด/ปิดสีแบรนด์) ให้ลูกค้ารู้ว่าร้านอยู่ตลาดไหน (ต้องไปรับที่ตลาดนั้นจริง)
   - `?role=market&market=ชื่อตลาด` (ลิงก์เฉพาะตลาดเดิม) **ยังทำงานปกติเหมือนเดิมทุกอย่าง** ไม่กระทบ
   - Commit `473b743` — เช็คแล้วว่า GitHub Actions deploy ผ่าน (`gh run list --workflow=deploy-worker.yml`)
4. **แก้ลิงก์ login เก่าที่ค้าง** — LINE rich menu ปุ่ม "เข้าสู่ระบบ" ใช้ LIFF ID `2010720123-HXe3iZJD` ที่ Endpoint URL ยังชี้ `kaidee-app.pages.dev` (โดเมน manual-deploy ที่ไม่มีใครกด deploy มา 2+ วัน ขาด fix paywall คืนก่อน) — ผู้ใช้ไปแก้ที่ **LINE Developers Console → Channel ":Done KaiDee" → LIFF → Endpoint URL** เป็น `https://kaidee-git.oneday-pos.workers.dev/index.html` เอง ยืนยันเสร็จแล้ว
5. **Security follow-up**: เช็ค `admin_access_log` ในโปรดักชันแล้ว — เจอแค่ entry เดียวจากวันที่ 2026-07-23 (ก่อนช่องโหว่ ADMIN_SECRET จะถูกแก้ และก่อนคืนตรวจสอบด้วยซ้ำ) กับยอด wallet ทุกบัญชีเป็นแค่ยอดทดสอบเล็กๆ (`pos:__qa_wallet` ฯลฯ) — **ไม่พบร่องรอยว่าช่องโหว่เคยถูกใช้โจมตีจริง**

### 🎨 ธุรกิจ/แบรนด์ — คุยแต่ยังไม่จบ
- **โลโก้**: ลองทำเวอร์ชัน `:D` (จุด+ตัว D โค้ง มาจาก `logo.jpg` เดิม) สีน้ำเงินให้ดู อ่านง่ายกว่า แต่ผู้ใช้ตัดสินใจสุดท้ายจะใช้ดีไซน์ตัวเอง **"Dne"** (ตัวอักษร D-วงกลม 2 วง(แทนตัว o)-ne + คำว่า KAIDEE เล็กๆ) แทน แม้จะแจ้งไปแล้วว่าเสี่ยงอ่านสับสนเป็น "Dne" โดยเฉพาะตอนย่อเล็ก — **เป็นการตัดสินใจของเจ้าของระบบ ไม่ต้องทักท้วงซ้ำอีกถ้า session หน้าเจอประเด็นนี้**
  - `logo.jpg` และ `kaidee-deploy/logo.jpg` ตอนนี้**มีการแก้เป็นเวอร์ชัน `:D` สีน้ำเงินค้างอยู่ในเครื่อง (uncommitted)** จากตอนที่ยังไม่ตกลงกันเรื่องดีไซน์ — **ต้อง revert หรือแทนที่ด้วยไฟล์ "Dne" ตัวจริงของผู้ใช้** (ผู้ใช้มีแค่สกรีนช็อตในแชท ยังไม่ได้ส่งไฟล์ export จริงมา) ก่อน commit ใดๆ เกี่ยวกับโลโก้
  - แยกให้ชัด: แบบพื้นขาว = ไอคอนแอป/โปรไฟล์ LINE OA, แบบพื้นน้ำเงินมีลายไอคอน = **Cover Photo ของ LINE OA** (คนละที่ตั้งค่ากันใน LINE OA Manager)
- **LINE OA rich menu**: ผู้ใช้ตัดสินใจอยากขยายจาก 2 ปุ่ม (ร้านค้า/เข้าสู่ระบบ) เป็น **3 ปุ่ม (ร้านค้า/ไรเดอร์/ลูกค้า)** — ให้ URL ไปแล้ว:
  - ร้านค้า → `https://kaidee-git.oneday-pos.workers.dev/index.html`
  - ไรเดอร์ → `https://kaidee-git.oneday-pos.workers.dev/Labor%20Win%20App%20v2.html?role=worker`
  - ลูกค้า → **ตอนนี้ควรเป็น** `https://kaidee-git.oneday-pos.workers.dev/index.html?role=market` (ไม่ใส่ `market=`) เพื่อเข้าหน้า "ร้านทั้งหมด" ใหม่ (ข้อ 3 ด้านบน) — ตอนคุยกันครั้งแรกยังไม่มีฟีเจอร์นี้ เคยแนะนำให้ชี้ไปตลาดสาธิตชั่วคราว **ให้ใช้ลิงก์ใหม่นี้แทน**
  - **ยังไม่ยืนยันว่าผู้ใช้ไปตั้งค่า 3 ปุ่มนี้ใน OA Manager จริงหรือยัง** — เช็คต่อ session หน้า
- **ข้อความทักทาย LINE OA**: ร่างตัวเลือกคำโปรยไว้ให้แล้ว (ทั้งแบบรวม 3 กลุ่ม และแยกร้านค้า/ไรเดอร์) แต่ **ยังไม่ได้เลือกตัวสุดท้าย/ยังไม่ได้ตั้งจริง**
- **คู่มือไรเดอร์ผิด — ยังไม่ได้แก้**: `kaidee-help.jsx` (คู่มือในแอปหลัก ผ่าน `HelpOverlay`) มีคู่มือครบทั้งร้านค้า/ลูกค้า/ไรเดอร์ แต่ **ส่วนไรเดอร์อธิบายระบบผิด** (อ้างอิง `kaidee-rider.jsx` แบบเก่า: อัปโหลด KYC, รอตรวจ 1-3 วัน, สูตรค่าเที่ยว ฿10+฿6.5/กม. — ไม่ตรงกับ Worker Pool จริงที่ใช้อยู่ตอนนี้ ซึ่งสมัครอิสระทันที ไม่มี KYC และแพลตฟอร์มไม่แตะเงินเลย) และ **`market-app.jsx` (แอปไรเดอร์/Labor Win ตัวจริง) ไม่มีคู่มือในแอปเลยสักบรรทัด** — งานที่ต้องทำ: เขียนคู่มือไรเดอร์ใหม่ให้ตรงกับ Pool จริง ใส่ใน `market-app.jsx`
- **แก้ไขข้อมูลจากรอบก่อนหน้า**: ที่เคยเข้าใจว่าแอปไรเดอร์จริงคือ `market-app.jsx` — **ผิด** เช็คซ้ำแล้วพบว่า `market-app.jsx` คือ Back Office ของ "เจ้าของตลาด/แผง" (OwnerDash, StallsView, BillingView ฯลฯ) ไม่มี role ไรเดอร์เลย **แอปไรเดอร์จริงคือ `laborwin2-worker.jsx`** (โหลดผ่าน `Labor Win App v2.html?role=worker`, ประกอบร่วมกับ `laborwin2-shop.jsx`/`laborwin2-win.jsx`/`platform-api.jsx`/`laborwin2-data.jsx`) — เช็คแล้วไม่มีคำว่า "คู่มือ"/"help" ในไฟล์นี้เลยเช่นกัน (สรุปเดิมที่ว่า "ไม่มีคู่มือไรเดอร์เลย" ยังถูกต้อง แค่ต้องไปแก้/เพิ่มที่ไฟล์ `laborwin2-worker.jsx` ไม่ใช่ `market-app.jsx`) — **ยังไม่ได้เขียนคู่มือ แค่แก้จุดที่ต้องไปทำให้ถูกไฟล์**
- **ค้างไม่ชัดเจน**: ผู้ใช้พิมพ์ "หน้าเติมเงินแพ็กเกจมีวิธีเดิมบอกก็ดีนะ" (เกี่ยวกับหน้าเลือกแพ็กเกจ/เติมเงิน) — ถามย้อนแล้วไม่ได้คำตอบชัดก่อน session ตัดจบ ต้องถามผู้ใช้ใหม่ว่าหมายถึงอะไร

### ℹ️ หมายเหตุอื่น
- ระหว่าง session เจอโฟลเดอร์โปรเจกต์ว่างเปล่าไปพักหนึ่ง (เหลือแค่ `.claude/`) ตกใจแต่เช็คแล้วเป็นแค่ sync เครื่อง/iCloud กระตุกชั่วคราว ไฟล์กลับมาครบปกติเอง **ไม่มีอะไรหายจริง** (และต่อให้หายจริง โค้ดก็ปลอดภัยบน GitHub อยู่แล้วเพราะ commit/push ไปตลอดระหว่างทาง)
- Session นี้จบเพราะ**โควต้าบัญชี Claude ใกล้หมด** ผู้ใช้อาจสลับไปใช้อีกบัญชีหนึ่งต่อ (`/logout` แล้ว `/login` ใหม่) — session ใหม่จะไม่มี context ของทั้งคืนนี้เลย นี่คือเหตุผลที่เขียนสรุปนี้ไว้

---

## 🗓️ Session 2026-08-01 ภาคค่ำ (ต่อ — คู่มือไรเดอร์ + เจอบั๊ก sync ข้อมูลร้าน)

### 🚨 บั๊กใหญ่ที่เจอตอนผู้ใช้แจ้ง "หน้าตั้งค่าเมนูใช้งานไม่ได้" — แก้แล้ว + apply ลง production DB แล้ว
**อาการจริงคือข้อมูลร้านไม่เคย sync ขึ้นเซิร์ฟเวอร์เลยตั้งแต่แรก** (ไม่ใช่แค่หน้าเมนู):
1. `POST /menu` INSERT คอลัมน์ `recipe`/`off` ที่**ไม่เคยมีอยู่จริงในตาราง `menu`** → 500 ทุกครั้ง ฝั่งแอป `.catch(()=>{})` กลืน error เงียบ → ตาราง `menu` ใน production **ว่างเปล่าทั้งตาราง** (0 แถว ทุกร้าน รวมร้านสาธิต)
2. ตาราง `settings` / `raw` / `purchases` / `cash_days` / `quotes` **ไม่เคยถูกสร้างใน D1 เลย** → ทุก endpoint เหล่านั้น 500 ตลอด (สต๊อกวัตถุดิบ ซื้อของเข้า ประวัติปิดวัน ใบเสนอราคา และ settings ทั้งชุด)
- ผลกระทบจริง: เมนู/สต๊อก/ตั้งค่า อยู่แค่ `localStorage` ของเครื่องเดียว — **ย้ายเครื่อง/ล้างแคช/เปิดอีกอุปกรณ์ = ข้อมูลไม่ตามไป** และ Back Office "จัดการเมนู (แก้แทนร้าน)" เห็นลิสต์ว่างเปล่า/เซฟไม่ได้
- แก้แล้ว: เพิ่ม ALTER/CREATE แบบ idempotent ใน `worker.js` (`ensureMenuConsignCols` + `ensureShopTables` ใหม่) และ **รัน migration บน D1 production จริงแล้ว** — ทดสอบ POST/PUT/GET ครบทั้ง 6 endpoint ผ่านหมด (commit `c68bb3c`, CI เขียว)
- แก้ตามมา: ลบเมนูแล้วไม่เคยลบบนเซิร์ฟเวอร์ (push เป็น upsert อย่างเดียว) — พอ sync ทำงานจริงเมนูที่ลบจะกลับมาตอนโหลดใหม่ → เพิ่มเรียก `DELETE /menu/:id` ตอนกดลบ (commit `253b718`)
- **ยังไม่ได้ทำ**: ข้อมูลของร้านจริงที่ค้างอยู่ในเครื่องยังไม่ถูก push ขึ้นไป — จะขึ้นเองเมื่อเจ้าของร้านแก้เมนู/ตั้งค่าครั้งถัดไป (effect ยิงตอน state เปลี่ยน) ถ้าอยากให้ขึ้นทันทีต้องเพิ่ม one-time backfill push ตอนเปิดแอป
- **บทเรียน**: ฝั่งแอปกลืน error API ทุกจุด (`.catch(()=>{})`) — บั๊กแบบนี้จะเงียบสนิท ควรตรวจ endpoint จริงด้วย curl เป็นระยะ ไม่เชื่อว่า "โค้ดมี = ทำงาน"

### ✅ คู่มือไรเดอร์ในแอป (งานที่ค้างจาก session ก่อน) — เสร็จแล้ว
- เพิ่มปุ่ม 📖 ข้างปุ่มเปลี่ยนภาษาในหน้าแรงงาน (`laborwin2-worker.jsx`) เปิดคู่มือ 8 หัวข้อ **3 ภาษา (ไทย/พม่า/เขมร)** ตรงกับระบบ Pool จริง: สมัครได้ทันทีไม่ต้องส่งเอกสาร, 2 สวิตช์, โหมดเวลาออโต้, งานตลาด vs งานนอก, **เงินสดจ่ายตรงมือ แพลตฟอร์มไม่ถือเงิน**, ค่าคอมหักเฉพาะคนที่เลือกสถานะสังกัดวิน (ดึง % จาก config จริง ไม่ hardcode), ขอเบิกล่วงหน้า, คะแนนดาว (commit `906178f`)
- sync `kaidee-deploy/` + bump `?v=` แล้ว

### ℹ️ เรื่องที่ตรวจแล้วพบว่าเข้าใจคลาดเคลื่อนใน handoff เดิม
- `kaidee-help.jsx` ส่วนไรเดอร์ **ไม่ได้ผิดไปเสียทั้งหมด** — มันอธิบาย `kaidee-rider.jsx` (ไรเดอร์ประจำร้าน ที่ยังโหลดอยู่ใน `index.html` และยัง reachable ผ่านลิงก์ไรเดอร์ของร้าน) ได้ตรงกับตัวแอปนั้นจริง (มีฟอร์ม KYC จริง, สูตรค่าเที่ยว `FARE.base` จริง, โบนัส 3 รอบจริง) — ที่ไม่ตรงคือ "ระบบไรเดอร์ของแพลตฟอร์ม" ซึ่งคือ Pool คนละตัวกัน
- หน้าตั้งค่าร้าน (`StoreScreen`) ตอนนี้ตั้ง `ridersComingSoon` เป็น true = การ์ด "ทีมส่ง · ไรเดอร์ร้าน" ขึ้นป้าย "เร็วๆ นี้" อยู่ → **ยังไม่ต้องรีบแก้คู่มือส่วนนั้น รอตัดสินใจก่อนว่าจะเก็บไรเดอร์ประจำร้านไว้ไหม**
- `assets/kaidee-logo.png` ถูก stage ไว้ว่า "ลบ" ทั้ง 3 ที่ (root/kaidee-deploy/market-deploy) ทั้งที่ยังมี ~20 หน้าอ้างอิงอยู่ (Back Office, Pitch Deck, Sponsor Console ฯลฯ) — **กู้กลับมาแล้ว** ถ้าตั้งใจจะเปลี่ยนเป็นโลโก้ใหม่ ต้องแทนที่ไฟล์ ไม่ใช่ลบทิ้ง

---

## 🗓️ Session 2026-08-02 (ต่อเนื่องข้ามคืน — ปิดช่องว่างระหว่างร้าน ↔ แพลตฟอร์ม ↔ ไรเดอร์)

### 🚨 บั๊กที่พบและแก้ (ทั้งหมดยืนยันบน production ด้วย curl/เบราว์เซอร์จริง)
1. **ข้อมูลร้านไม่เคย sync ขึ้นเซิร์ฟเวอร์เลย** — `POST /menu` INSERT คอลัมน์ `recipe`/`off` ที่ไม่มีในตาราง → 500 ทุกครั้ง ตาราง `menu` ว่างทั้งตาราง · ตาราง `settings`/`raw`/`purchases`/`cash_days`/`quotes` ไม่เคยถูกสร้างใน D1 เลย → ทุก endpoint 500 · แก้แล้ว + apply migration บน production (`c68bb3c`)
2. **ลบเมนูแล้วไม่ลบบนเซิร์ฟเวอร์** (push เป็น upsert อย่างเดียว) พอ sync ทำงานจริงเมนูที่ลบจะกลับมา → เพิ่ม `DELETE /menu/:id` ตอนกดลบ (`253b718`)
3. **หน้าแรกไม่เคยขึ้นหน้า "เลือกระบบ"** — `kaidee-api.jsx` เขียน `kd_shop='kaidee'` (ค่า fallback) ลง localStorage ทุกครั้งที่โหลด แล้ว router เช็คแค่ว่า "มี kd_shop ไหม" → ข้าม Signup Chooser ตลอดกาลสำหรับทุกคน · แก้ 2 ชั้น (ไม่เขียน fallback + guard มองข้ามค่า 'kaidee' ที่ค้าง) (`72f48dd`)
4. **ปุ่ม disabled ในหน้าไรเดอร์กดไม่ได้ทั้งหมด** — helper `h()` เขียน `setAttribute('disabled', false)` ซึ่ง HTML ถือว่า disabled (`7b8737b`)
5. **POST /pool/job ทิ้งข้อมูลงานส่ง** — เบอร์ลูกค้า/ที่อยู่ร้าน/ยอดเก็บปลายทางหายตอนบันทึก ไรเดอร์รับงานแล้วทำต่อไม่ได้ (`bac596a`)

### ✅ ฟีเจอร์ใหม่ที่เสร็จ + deploy แล้ว
1. **ช่องทางขาย "ขายบน :Done KaiDee"** (`market`) — เปิดให้ทุกร้านโดยอัตโนมัติ ติ๊กรายเมนูได้ในหน้าแก้เมนู · **ราคาล็อกเท่าช่องทาง "กลับบ้าน" (takeaway) เสมอ** บวกเพิ่มไม่ได้ ปลดล็อกเป็นรายร้านได้จาก Back Office (`features.marketPrice`)
2. **ร้านเข้าร่วมแพลตฟอร์มเองได้** — การ์ด "ตลาด/ทำเลของร้าน" ในหน้าตั้งค่าร้าน (เลือก/พิมพ์ชื่อตลาด → `PATCH /shops/:id {market}`) + **สวิตช์พักรับออเดอร์บนแพลตฟอร์มแยกจากเปิด/ปิดร้าน** (`marketOpen`)
3. **หน้าร้านฝั่งลูกค้าสไตล์ LINE MAN** (`kaidee-market-shop.jsx`) ใช้เฉพาะ `?via=market` — ปกร้าน ป้ายสถานะ ค้นหาเมนู หมวดแบบ section การ์ดเมนู ป้ายสินค้าหมด แถบตะกร้าลอย · ตะกร้า/จ่ายเงิน/ติดตามใช้ของเดิมทั้งหมด (ไม่แตะโค้ดเงิน)
4. **สมัครไรเดอร์แพลตฟอร์ม** (`Rider Signup.html`) แบบ Grab/LINE MAN 4 ขั้น + ถ่ายเอกสาร 6 อย่าง (ย่อรูปในเครื่องก่อนส่ง) → `POST /riders` บน platform-worker · เช็คสถานะด้วยเบอร์ได้
5. **หน้าตรวจใบสมัครใน Back Office** — แท็บ "🛵 ใบสมัครไรเดอร์" ดูรูปเอกสารซูมได้ อนุมัติ/ไม่ผ่าน (พิมพ์เหตุผลให้ผู้สมัครเห็น) · ล็อกอินด้วยรหัสแอดมิน **platform-worker** (คนละตัวกับ POS) เก็บ token ไว้ในเครื่อง
6. **แอปไรเดอร์** (`Rider App.html`) — เข้าด้วยเบอร์ที่อนุมัติแล้ว · สวิตช์เปิด/ปิดรับงาน (ปักหมุด GPS) · งานรอบตัวพร้อมระยะทาง/ค่าส่ง/ป้ายเก็บเงินปลายทาง · รับงาน → ถึงร้าน → ส่งสำเร็จ · โทรลูกค้า/นำทาง · สรุปรายได้วันนี้
7. **ร้านเรียกไรเดอร์ได้จากออเดอร์** — ปุ่ม "🛵 เรียกไรเดอร์" บนออเดอร์เดลิเวอรี → `POST /pool/job?region=delivery` (region แยกจาก pool ของระบบวินคนละชุด) กันเรียกซ้ำด้วย `o.riderJob`
8. **หน้าแรกสมัคร** โชว์ครบ: ร้านค้า/ฟิตเนส/ตลาด/วิน/**สมัครไรเดอร์**/สปอนเซอร์ · หน้าเลือกประเภทธุรกิจในแอปเลิกซ่อนฟิตเนส+วิน
9. **โลโก้ใหม่ (smiley-D)** ใช้ทั้ง `logo.jpg` (ไอคอนแอป) และ `assets/kaidee-logo.png` (Back Office/เด็ค) + bump SW cache เป็น v64
10. **คู่มือไรเดอร์ในแอป Labor Win** 8 หัวข้อ 3 ภาษา (ไทย/พม่า/เขมร) ปุ่ม 📖 ข้างปุ่มเปลี่ยนภาษา
11. **เติมเมนูร้านสาธิตครบ 13 ร้าน 78 รายการ** ขึ้น production แล้ว (ก่อนหน้านี้ลูกค้าเห็นเมนูตัวอย่างของแอป)
12. ซ่อนปุ่มลอยสลับบทบาท (ของเดโม) จากลูกค้าที่เข้ามาทางแพลตฟอร์ม — เดิมกดแล้วหลุดเข้าจอ POS ของร้านได้

### ✅ รอบเสริม (ต่อจากด้านบน — ความปลอดภัย/ประสบการณ์ใช้งาน)
- **ปิดช่องโหว่แอดมิน 13 จุด** ใน `kaidee-pos-worker`: pattern `if (env.ADMIN_SECRET && !verifyToken(...))` → ถ้า secret ไม่ถูกตั้ง = ข้ามการเช็คทั้งหมด · เปลี่ยนเป็นเช็ค token ตรงๆ ทุกจุด (ทดสอบ: ปลอม token จาก fallback secret ที่ public บน GitHub → 403)
- **`POST /biz` / `PATCH /biz/:id` ล็อกแล้ว** — สร้างใหม่/เคลมของที่ยังไม่มีเจ้าของยังเปิด (bottom-up) แต่ทะเบียนที่มีเจ้าของแล้วต้องเป็นเจ้าของเดิม (ownerLine ตรง) หรือแอดมิน
- **บั๊กเศษสตางค์ wallet**: จับคู่เงินโอนเข้ากับ topup ที่ pending เคยปัดยอดข้างเดียวแล้วยอมคลาด 0.5 → ยอดลงท้าย `.50` ไม่มีวันจับคู่ได้ และยอดต่างกัน ≤0.49 กลับจับคู่ให้ (เข้าผิดร้านได้) · **ตามที่เจ้าของระบบสั่ง: ปัดเป็นบาทเต็มทั้งสองฝั่ง** (โอน 500.25/499.75 = 500) ยอดเข้ากระเป๋า = ยอดที่ขอเติม ส่วนต่างบันทึกใน `reason` · บั๊กเดียวกันที่จับคู่สลิปกับบิลขายก็แก้แล้ว (ฝั่งบิลยังเผื่อ 0.5 ตามเดิม แต่เทียบยอดจริงไม่ปัดก่อน)
- **กู้ร้านคืน** — หน้าแรกมีปุ่ม "มีร้านอยู่แล้ว?" เข้าด้วย LINE ของเจ้าของ หรือ รหัสร้าน + PIN (`/shops/:id/owner-login`) · เพิ่มที่ตั้ง PIN ในหน้าตั้งค่าร้าน (`OwnerPinSheet`) เพราะเดิมไม่มี UI ตั้ง PIN เลย ทำให้ทางกู้ด้วย PIN ใช้ไม่ได้จริง
- **หน้าแรกสมัครใหม่ทั้งหน้า** — ถาม "คุณเป็นใคร" (ร้านค้า/ตลาด/จ้างแรงงาน + ไรเดอร์/สปอนเซอร์) · ร้านค้ายุบเป็นทางเดียวแล้วเลือกประเภทร้าน (`?type=` ส่งต่อให้ CRM ข้ามขั้นถามซ้ำ + ได้เมนูตั้งต้นตามประเภท) · ฝั่งวินเปลี่ยนคำเป็น "ฉันเป็นแม่ค้า/ร้านในตลาด" ให้แม่ค้ารู้ว่าต้องกดตรงไหน
- **คำโปรยเดลิเวอรี** — บอกชัดว่า "ไม่ได้รับออเดอร์จาก Grab/LINE MAN โดยตรง" แค่บันทึกยอดรวมที่เดียว + เทียบยอดโอน · คำโปรยแพ็กคำนวณจาก seats จริงจากเซิร์ฟเวอร์ (เดิมเขียน "หลายเครื่อง" ทั้งที่แพ็ก 1 เครื่อง)
- **ลดเวลาเปิดแอปครั้งแรก** — React dev build → production (−1MB) + จอฝั่งลูกค้า (`role=customer|market|board|kds`) ไม่โหลด/ไม่คอมไพล์โค้ดฝั่งร้าน (merchant2 522KB ฯลฯ) · วัดจริง: **6056KB → 4272KB (−30%)**, ไฟล์ JSX 23 → 18
  - **ยังเหลือคอขวดใหญ่คือ `babel.min.js` 3MB** ที่คอมไพล์ JSX ในเบราว์เซอร์ตอนรัน → เวลาเปิดยังราว 6.8 วิ **แก้จริงต้องมี build step** (esbuild แปลง .jsx → .js ตอน deploy) ซึ่งเปลี่ยนวิธี deploy ต้องให้เจ้าของระบบตัดสินใจก่อน
  - `STATUS_LABEL` ย้ายจาก `kaidee-merchant2.jsx` → `kaidee-data.jsx` แล้ว (จอลูกค้าใช้ตัวนี้ตัวเดียว ถ้าไม่ย้ายจะตัดโค้ดฝั่งร้านออกไม่ได้)

### 🔑 สถาปัตยกรรมที่ต้องจำ
- **ไรเดอร์แพลตฟอร์ม ≠ ระบบวิน (Labor Win) ≠ ไรเดอร์ประจำร้าน** — 3 ระบบแยกกันสนิท:
  - แพลตฟอร์ม: `Rider Signup.html` + `Rider App.html` → platform-worker `coll='riders'` และงานที่ `region=delivery`
  - วิน: `Labor Win App v2.html` → `region=pool` (ค่า default) มีหัวคิว/ค่าคอม 2%
  - ประจำร้าน: `kaidee-rider.jsx` (`?shop=x&role=rider`) — **ฟอร์มสมัครเป็นของจำลอง ไม่ต่อหลังบ้าน** ยังไม่ตัดสินใจว่าจะทำยังไงต่อ
- หน้าแรงงานในแอปวินมีแค่**ลิงก์**ไปสมัครไรเดอร์แพลตฟอร์ม ไม่แชร์ข้อมูลกัน

---

## ⚠️ หมายเหตุสำคัญสำหรับ session ถัดไป
- **Deploy pipeline คืนนี้แก้เสร็จและเชื่อถือได้แล้ว** — push ขึ้น `main` แล้ว deploy อัตโนมัติจริง (เคยพังมาก่อน อย่าสันนิษฐานว่า "push แล้ว = ขึ้นจริง" โดยไม่เช็ค `gh run list --workflow=deploy-worker.yml` ก่อน แม้จะแก้แล้วก็ตาม เป็นนิสัยที่ดี)
- **root กับ `kaidee-deploy/` ต้อง sync มือทุกครั้งที่แก้ frontend .jsx/index.html** — ไม่มี build step อัตโนมัติ ลืม sync ไฟล์จะไม่อัปเดตจริงแม้ push ไปแล้ว
- **`kaidee-pos-worker`, `platform-worker`, `license-worker` เป็นคนละ D1 database คนละ deploy กัน** — อย่าสับสนว่าแก้ worker หนึ่งแล้วอีกตัวจะได้ผลด้วย
- ก่อนแก้ endpoint แอดมินใหม่ในอนาคต **ห้ามใช้ pattern `if (env.ADMIN_SECRET && !verifyToken(...))`** — ใช้ `if (!verifyToken(...))` ตรงๆ เสมอ (แบบ platform-worker ทำถูกอยู่แล้วในบางจุด) ป้องกันบั๊กเดิมเกิดซ้ำ

---

## 🗓️ Session 2026-08-03 (ทำฝั่งแพลตฟอร์มให้ใกล้ LINE MAN)

### ✅ เสร็จ + ขึ้น production แล้ว (ทดสอบด้วย curl จริงและเบราว์เซอร์จริงทุกข้อ)
1. **ระบบโปร/คูปองของร้าน (สร้างใหม่ทั้งหมด — เดิมไม่มีเลย)** — ตาราง `promos` + `promo_uses`
   5 แบบ: ลด% · ลดบาท · ส่งฟรี · ราคาพิเศษ · ซื้อ X แถม Y
   ตั้งได้: ขั้นต่ำ · เพดานส่วนลด · เมนู/หมวดที่เข้าโปร · ช่องทาง · ช่วงวัน-เวลา · โควตารวม/ต่อคน · ซ้อนกับส่วนลดสมาชิกได้ไหม
   หน้าสร้างโปรอยู่ใน **ตั้งค่าร้าน → โปรโมชั่น & คูปอง**
   **ยอดคิดใหม่ที่เซิร์ฟเวอร์เสมอ** — ทดสอบแล้วว่าแก้ total เป็น ฿1 ในเบราว์เซอร์ เซิร์ฟเวอร์ดันกลับเป็น ฿160
2. **หน้ากดส่วนลดตอนจ่ายเงิน** — แยก "ใช้ได้กับบิลนี้" / "ยังใช้ไม่ได้" พร้อมเหตุผล (สั่งเพิ่มอีก ฿150 · ใช้กับช่องทางนี้ไม่ได้ · ยังไม่มีเมนูที่เข้าโปรในตะกร้า) · สรุปบิลแยกบรรทัด + ป้าย "ประหยัดไป ฿XX"
3. **ป้ายโปรบนการ์ดร้าน + แถบโปรในหน้าร้าน + ป้ายรายเมนู** — เอาเฉพาะใบที่ลดอัตโนมัติมาโฆษณา (ใบที่ต้องกรอกโค้ดไม่โชว์ เพราะลูกค้าไม่รู้โค้ด)
4. **ที่อยู่ลูกค้า + ร้านใกล้ฉัน** — ปักหมุด GPS ครั้งเดียว การ์ดร้านขึ้นระยะทาง/เวลา/ค่าส่งจริง เรียงใกล้สุดได้ · ร้านที่ไม่ได้ปักหมุดไปท้ายลิสต์เสมอ · เก็บในเครื่องเท่านั้น
5. **สมุดที่อยู่** — จำเฉพาะที่อยู่ที่เคยสั่งจริง แตะเลือกได้
6. **แถบเมนูล่าง + ออเดอร์ของฉัน (รวมทุกร้าน)** — `GET /my-orders?line=` ค้นด้วย LINE userId เท่านั้น (ห้ามเปิดให้ค้นด้วยเบอร์)
7. **ติดตามไรเดอร์จริง** — ผูกออเดอร์กับงานไรเดอร์ข้าม worker · สถานะจริง ชื่อ ทะเบียน ปุ่มโทร ระยะที่เหลือจากพิกัดจริง · แอปไรเดอร์ส่งพิกัดทุก 20 วิระหว่างวิ่งงาน
8. **ร้านโปรด (หัวใจ)** + ชิปกรอง
9. **คะแนนดาว/รีวิว** — ให้ได้เฉพาะคนที่สั่งจริงและบิลปิดแล้ว 1 บิล 1 รีวิว · ต่ำกว่า 3 รีวิวไม่โชว์ดาว
10. **อัปโหลดรูปขึ้น R2** (ปก 1200px · โลโก้ 512px · เมนู 800px) เก็บเป็นลิงก์
11. **แจ้งเตือนไรเดอร์ผ่าน Web Push** (ฟรีไม่จำกัด) แทน LINE ที่คิดเงินรายข้อความ + ปุ่มติดตั้งแอปในหน้าสมัคร

### 🐛 บั๊กที่เจอระหว่างทาง (ไม่ได้ตั้งใจหา แต่บล็อกงานอยู่)
1. **แอปร้านขึ้นจอขาวทั้งแอป** ถ้ามีออเดอร์แม้บิลเดียวที่เก็บรายการสินค้าคนละรูปแบบ — ล้างแคชไม่หายเพราะข้อมูลอยู่บนเซิร์ฟเวอร์ · แก้แล้วโดยบังคับรูปแบบทั้งตอนเขียนและตอนอ่าน
2. **ลูกค้ากดร้านจากหน้ารวมร้านแล้วเด้งกลับหน้ารวมทุกครั้ง สั่งอาหารไม่ได้เลย** — ธงที่จำไว้กู้ตอน LINE ล้างพารามิเตอร์ ไม่เคยถูกล้างเมื่อเจาะเข้าร้าน · **ยิง API ยังไงก็ไม่เจอ ต้องกดในเบราว์เซอร์จริง**
3. **หน้าจ่ายเงินมีที่อยู่ตัวอย่างกรอกไว้ให้แล้ว** (`คอนโด The Nest ลาดพร้าว ห้อง 812`) ลูกค้าที่กดผ่านเร็วจะสั่งไปที่อยู่ที่ไม่มีจริง
4. **หน้าร้านโชว์ ★4.8 เท่ากันทุกร้าน** เป็นเลขที่พิมพ์ค้างไว้ในโค้ด
5. **รูปเมนูยัดรูปดิบจากกล้อง (หลายเมกะไบต์) ลงข้อมูลเมนูตรง ๆ** ไม่ย่อเลย
6. **worker เรียก worker ผ่าน URL สาธารณะของบัญชีตัวเองถูกบล็อก** — ยิงจากข้างนอกได้ 200 ปกติ จึงไม่เห็นตอนทดสอบด้วย curl อย่างเดียว · ต้องผูกเป็น service binding

### 🧠 การตัดสินใจที่คุยกันคืนนี้
- **ไม่ย้ายไป Java/Kotlin** — Grab ใช้ Go, LINE MAN Wongnai ใช้ Kotlin+Spring แต่เขามีวิศวกรหลายร้อยคน เหตุผลคือขนาดทีม ไม่ใช่ภาษาเร็วกว่า · คอขวดตอนนี้คือยังไม่มีร้านจริงใช้งาน
- **ไม่ให้เครดิตไรเดอร์ฝั่งแพลตฟอร์ม** — ระบบวินมีเครดิต/เบิกล่วงหน้าครบอยู่แล้วเพราะเงินไม่ผ่านมือ KaiDee (เป็นหนี้ระหว่างร้าน↔หัวหน้าวิน↔ลูกทีม) ถ้าแพลตฟอร์มออกเงินให้ก่อนเอง = ปล่อยสินเชื่อ ต้องขอใบอนุญาต ธปท.
- **ตลาดสด: ลูกค้าโอนตรงแผงตอนชั่งเสร็จ** ไม่ใช่ตอนสั่ง (ยังไม่รู้ราคาจริง) และไม่ใช่ให้คนช่วยซื้อควักก่อน — คนซื้อถ่ายรูป+ใส่ยอดจริง → แอปเด้ง QR ของแผงนั้น → ลูกค้าจ่าย → คนซื้อรับของ · แผงที่ไม่มีพร้อมเพย์ค่อยใช้มัดจำ
